# Pharmax — Dark Mode Pharmacy Management Front-End

Portfolio-ready React SPA (front-end only) showcasing a dark-mode, purple-themed Pharmacy Management System UI.

Quick start:

```bash
cd pharmax
npm install
npm run dev
```

Open http://localhost:5173

Notes:
- All data is mocked/hardcoded for UI purposes only.
- Built with Vite, React, Chart.js. No backend or auth.
